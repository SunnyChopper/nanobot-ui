/**
 * Re-export from pages for backward-compatible imports (e.g. Layout).
 * Workflows UI is implemented under Atomic Design: atoms, molecules, organisms, pages.
 */
export { WorkflowsPage } from './pages/WorkflowsPage'
