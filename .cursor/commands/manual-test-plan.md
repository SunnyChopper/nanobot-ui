Based on this conversation thread, create a very short list of acceptance criteria to ensure everything was built correctly. I can test these things manually. Do NOT write to a new file, just return the list of acceptance criteria. Use clear ASCII formatting to indicate dependencies and ordered steps as needed. Focus ONLY on what can be tested through using the application itself. So, exclude things like "ensure the code is correct" or "ensure the documentation is up to date" or any API testing with Postman.

Expected output format:

Acceptance Criteria Checklist:
---------------------------------
1. [ ] Top-level acceptance criterion
2. [ ] Next criterion
    └── [ ] Sub-step, if any
3. [ ] Another criterion
    ├── [ ] First dependent task
    └── [ ] Second dependent task
...

(Use ASCII lines └──, ├──, and indentation to show dependencies or subtasks beneath checklist items; use numbered or bulleted lists consistently. Keep it readable in plain text.)